<% @page_title = "Edit Page" %>

<%= link_to("<< Back to List", pages_path, :class => 'back_link') %>

<div class="pages edit">
  <H2>Edit Page</H2>

  <%= form_for(@page) do |f| %>

      <%= render(:partial => 'form', :locals => {:f => f}) %>

      <div class="form-buttons">
        <%= f.submit("Update Page") %>
      </div>

  <% end %>

</div>
